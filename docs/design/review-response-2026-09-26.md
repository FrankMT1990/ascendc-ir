# 复审响应（2026-09-26）

对应 `docs/design/review-findings-2026-09-26.md`（复审对象 `e17705b`）。

## 结论总览

两条「还要改」全部属实，已修复。核对项（event id 运行时三元表达式）用更彻底的方式消除：循环按槽位展开，event id 全为编译期字面量（ADR 0011）。

## 还要改逐条

| # | 结论 | 处置 |
|---|---|---|
| 1 | 同一槽位被两条计算读时，WAR 在第一次读后就释放，且同一 id 连续 notify | 接受。发射端预计算每 buffer 在循环体内的「最后读语句下标」，只在最后一次消费之后释放，每轮每个 buffer 一次；新增 `test_war_release_after_last_read`（两条 add 都读完才释放） |
| 2 | 就地写被「每槽位至多写一次」误伤，检查器放行、codegen 拒绝，两边不一致 | 接受。重卷的复用判定排除就地 RMW（读改写同槽位不算循环证据）；发射端以「生产 PIPE 必须唯一」给出明确报错；`ir-syntax.md` 写明 v0.2 不支持跨 PIPE 就地改写 |

## 核对项

| 项 | 处置 |
|---|---|
| event id 运行时三元表达式可能编不过 | 不再依赖该问题的答案：循环按槽位展开（`i += N`），event id、槽位偏移、WAR 守卫全部编译期字面量。新约束 TILES % lcm(stages) == 0，违反拒绝生成。ADR 0011 |

## 验证

- `pytest tests/`：46 passed（新增：最后读之后释放、RMW 明确报错、整除约束；更新：结构断言、去重计数）
- `evals/m4_m5/run_injection.py`：合法 3/3、召回 9/9、定位 9/9、干净 9/9
- `add_golden.asc` 与 `add_reference.asc` 同步重写为按槽位展开形态
